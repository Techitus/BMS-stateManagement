/* eslint-disable react-refresh/only-export-components */
import React from 'react'
const Title = () => {
  return (
    <div>
        <h1>Learnig useCallback</h1>
    </div>
  )
}

export default React.memo(Title)